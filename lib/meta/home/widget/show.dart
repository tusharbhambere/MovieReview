import 'package:flutter/material.dart';

class ShowTab extends StatelessWidget {
  const ShowTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
