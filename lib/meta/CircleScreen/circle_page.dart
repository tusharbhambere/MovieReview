import 'package:flutter/material.dart';

class CirclePage extends StatelessWidget {
  static const route = '/circle_page';
  const CirclePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
