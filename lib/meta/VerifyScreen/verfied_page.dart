import 'package:flutter/material.dart';

class VerifiedPage extends StatelessWidget {
  static const route = '/verified_page';
  const VerifiedPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
